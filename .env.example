# Upstream API gateway — Vite dev server (port 3000) proxies /api and /socket.io here.
# Switch environments by changing this value (no trailing slash).
#
# Live dev:  https://saasppsgateway.eglobeitsolutions.org
# Local:     http://localhost:8080
VITE_API_GATEWAY_URL=https://saasppsgateway.eglobeitsolutions.org
