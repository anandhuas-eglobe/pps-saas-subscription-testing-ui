# Leave empty to use Vite dev proxy (/api -> http://localhost:3112)
VITE_API_BASE_URL=

# Leave empty to use Vite dev proxy (/api/v1/auth -> http://localhost:3104)
# Set only if calling IAM directly (e.g. http://localhost:3104)
VITE_IAM_BASE_URL=
